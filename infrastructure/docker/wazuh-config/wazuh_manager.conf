<ossec_config>

  <!-- ======================================== -->
  <!-- GLOBAL                                   -->
  <!-- ======================================== -->
  <global>
    <jsonout_output>yes</jsonout_output>
    <alerts_log>yes</alerts_log>
    <logall>no</logall>
    <logall_json>no</logall_json>
    <email_notification>no</email_notification>
    <rotate_log>yes</rotate_log>
    <max_output_size>10M</max_output_size>
  </global>

  <!-- ======================================== -->
  <!-- INGESTION LOGS SURICATA EVE.JSON         -->
  <!-- ======================================== -->
  <localfile>
    <log_format>json</log_format>
    <location>/var/ossec/logs/suricata/eve.json</location>
    <label key="source">suricata</label>
  </localfile>

  <!-- ======================================== -->
  <!-- CLUSTER (single-node)                    -->
  <!-- ======================================== -->
  <cluster>
    <name>wazuh</name>
    <node_name>master-node</node_name>
    <node_type>master</node_type>
    <key>c98b62a9993b2b08</key>
    <port>1516</port>
    <bind_addr>0.0.0.0</bind_addr>
    <nodes>
      <node>wazuh.manager</node>
    </nodes>
    <hidden>no</hidden>
    <disabled>yes</disabled>
  </cluster>

  <!-- ======================================== -->
  <!-- RÈGLES                                   -->
  <!-- ======================================== -->
  <ruleset>
    <decoder_dir>ruleset/decoders</decoder_dir>
    <rule_dir>ruleset/rules</rule_dir>
    <rule_exclude>0215-policy_rules.xml</rule_exclude>
    <list>etc/lists/audit-keys</list>
    <list>etc/lists/security-eventchannel</list>
    <list>etc/lists/amazon/aws-eventnames</list>
    <user_rules>etc/rules/local_rules.xml</user_rules>
  </ruleset>

  <!-- ======================================== -->
  <!-- API                                      -->
  <!-- ======================================== -->
  <api>
    <behind_proxy_server>no</behind_proxy_server>
  </api>

  <!-- ======================================== -->
  <!-- INDEXER (OpenSearch)                     -->
  <!-- ======================================== -->
  <indexer>
    <enabled>yes</enabled>
    <hosts>
      <host>https://wazuh.indexer:9200</host>
    </hosts>
    <ssl>
      <certificate_authorities>
        <ca>/etc/ssl/root-ca.pem</ca>
      </certificate_authorities>
      <certificate>/etc/ssl/filebeat.pem</certificate>
      <key>/etc/ssl/filebeat.key</key>
    </ssl>
  </indexer>

</ossec_config>