\chapter{IntÃ©gration SIEM avec Wazuh}

\section{PrÃ©sentation de Wazuh}

Wazuh est une plateforme SIEM open-source (fork d'OSSEC) offrant une analyse en temps rÃ©el des Ã©vÃ©nements de sÃ©curitÃ©, une dÃ©tection d'intrusion basÃ©e sur des rÃ¨gles et une conformitÃ© rÃ©glementaire (PCI-DSS, GDPR). Dans ce projet, il sert de point central d'agrÃ©gation et de corrÃ©lation des Ã©vÃ©nements gÃ©nÃ©rÃ©s par Suricata, Mosquitto et les conteneurs Docker.

\section{DÃ©ploiement de Wazuh OVA}

\subsection{Choix du mode de dÃ©ploiement}

AprÃ¨s avoir Ã©valuÃ© l'option Docker Compose (qui prÃ©sentait des incompatibilitÃ©s avec l'environnement GNS3), le dÃ©ploiement final retenu est l'OVA officielle Wazuh v4.7 sur VMware Workstation Pro.

\textbf{Configuration de la VM Wazuh :}
\begin{itemize}
  \item \textbf{Adresse IP} : 192.168.30.128
  \item \textbf{RAM} : 8 GB
  \item \textbf{vCPU} : 4
  \item \textbf{Stockage} : 50 GB
  \item \textbf{Dashboard} : \texttt{https://192.168.30.128}
  \item \textbf{Credentials} : \texttt{admin} / \texttt{*Admin1234!}
\end{itemize}

\subsection{Architecture Wazuh}

La solution Wazuh dÃ©ployÃ©e comprend une architecture single-node :
\begin{itemize}
  \item \textbf{Wazuh Manager} : Moteur de rÃ¨gles, corrÃ©lation, agrÃ©gation.
  \item \textbf{Wazuh Indexer} : Stockage et indexation (OpenSearch).
  \item \textbf{Wazuh Dashboard} : Interface de visualisation (OpenSearch Dashboards).
\end{itemize}

\section{Configuration de l'ingestion Suricata}

\subsection{Configuration du logcollector}

L'agent Wazuh Manager est configurÃ© pour surveiller le fichier EVE JSON de Suricata en mode \texttt{tail} :

\begin{lstlisting}[language=xml, caption=Configuration ossec.conf pour l'ingestion Suricata]
<ossec_config>
  <localfile>
    <log_format>json</log_format>
    <location>/var/log/suricata/eve.json</location>
    <query>
      $.event_type == "alert"
    </query>
  </localfile>
</ossec_config>
\end{lstlisting}

\subsection{RÃ¨gles de corrÃ©lation personnalisÃ©es}

Quatre rÃ¨gles custom (IDs 100200-100203) ont Ã©tÃ© crÃ©Ã©es dans \texttt{/var/ossec/etc/rules/local\_rules.xml} pour Ã©lever le niveau de sÃ©vÃ©ritÃ© des alertes Suricata :

\begin{lstlisting}[language=xml, caption=RÃ¨gles Wazuh personnalisÃ©es pour les alertes IoT]
<group name="suricata,iot,">
  <!-- Alerte TLS sur port MQTT -->
  <rule id="100200" level="3">
    <if_group>suricata</if_group>
    <field name="dest_port">8883</field>
    <description>Suricata: TLS MQTT traffic alert</description>
    <group>tls,mqtt,</group>
  </rule>

  <!-- Alerte gÃ©nÃ©rique Suricata -->
  <rule id="100201" level="6">
    <if_group>suricata</if_group>
    <field name="alert.severity">^[123]$</field>
    <description>Suricata: $(alert.signature)</description>
    <group>ids,</group>
  </rule>

  <!-- Attaque DoS IoT -->
  <rule id="100202" level="10">
    <if_sid>100201</if_sid>
    <field name="alert.category">Denial of Service</field>
    <description>IoT DoS Attack detected: $(alert.signature)</description>
    <group>dos,attack,</group>
  </rule>

  <!-- Volume Ã©levÃ© d'alertes -->
  <rule id="100203" level="8" frequency="10" timeframe="60">
    <if_matched_sid>100201</if_matched_sid>
    <same_field>src_ip</same_field>
    <description>High volume of IDS alerts from $(src_ip)</description>
    <group>correlation,</group>
  </rule>
</group>
\end{lstlisting}

\section{Visualisation dans le dashboard}

\subsection{Dashboard Wazuh â€” rÃ©sultats}

Les 51 alertes Suricata ont Ã©tÃ© correctement ingÃ©rÃ©es et indexÃ©es dans Wazuh. Elles sont visibles via le filtre \texttt{rule.groups: suricata} dans l'interface de dÃ©couverte.

\begin{figure}[H]
  \centering
  % \includegraphics[width=\textwidth]{figures/wazuh-dashboard.png}
  \fbox{\parbox{0.9\textwidth}{\centering\vspace{2cm}\textit{[Figure : Dashboard Wazuh avec alertes Suricata â€” capture d'Ã©cran Ã  insÃ©rer]}\vspace{2cm}}}
  \caption{Dashboard Wazuh affichant les alertes IoT/MQTT}
  \label{fig:wazuh_dashboard}
\end{figure}

\subsection{Distribution des alertes par niveau}

\begin{table}[H]
\centering
\caption{Distribution des alertes par niveau de sÃ©vÃ©ritÃ© Wazuh}
\label{tab:wazuh_levels}
\begin{tabular}{lll}
\toprule
\textbf{Niveau} & \textbf{RÃ¨gle} & \textbf{Alertes} \\
\midrule
3 (Low) & 100200 â€” TLS MQTT & 18 \\
6 (Medium) & 100201 â€” Generic Suricata & 20 \\
8 (High) & 100203 â€” High volume & 9 \\
10 (Critical) & 100202 â€” DoS Attack & 4 \\
\bottomrule
\end{tabular}
\end{table}

\begin{tcolorbox}[colback=green!5, colframe=green!50!black, title=RÃ©sultats Wazuh SIEM]
\begin{itemize}
  \item \textbf{51 alertes Suricata} ingÃ©rÃ©es et indexÃ©es avec succÃ¨s.
  \item \textbf{4 alertes critiques} (niveau 10) pour les attaques DoS â€” escalade correctement appliquÃ©e.
  \item CorrÃ©lation multi-sources opÃ©rationnelle (Suricata + logs systÃ¨me).
  \item Dashboard visible Ã  \texttt{https://192.168.30.128} avec visualisations temps-rÃ©el.
\end{itemize}
\end{tcolorbox}
