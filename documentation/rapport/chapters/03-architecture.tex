\chapter{Architecture GÃ©nÃ©rale du SystÃ¨me}

\section{Vue d'ensemble}

L'architecture retenue pour ce projet est basÃ©e sur une approche \textbf{dÃ©fense en profondeur} (\textit{defense in depth}) : chaque couche de l'Ã©cosystÃ¨me dispose de ses propres mÃ©canismes de sÃ©curitÃ©, de sorte qu'une compromission partielle ne compromet pas l'ensemble du systÃ¨me.

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figures/architecture-generale.png}
  \caption{Architecture gÃ©nÃ©rale de l'Ã©cosystÃ¨me IoT sÃ©curisÃ©}
  \label{fig:archi_generale}
\end{figure}

\section{Topologie rÃ©seau GNS3}

La simulation rÃ©seau est rÃ©alisÃ©e sous \textbf{GNS3} (Graphical Network Simulator 3) avec VMware Workstation Pro comme hyperviseur. L'environnement comprend :

\subsection{Segmentation rÃ©seau}

\begin{table}[H]
\centering
\caption{Plan d'adressage de la topologie GNS3}
\label{tab:adressage}
\begin{tabular}{lllp{5cm}}
\toprule
\textbf{RÃ©seau} & \textbf{CIDR} & \textbf{Composants} & \textbf{RÃ´le} \\
\midrule
IoT Devices & 192.168.10.0/24 & fleet-simulator, capteurs & Zone devices â€” trafic MQTT non chiffrÃ© interne \\
Services & 192.168.20.0/24 & Mosquitto, Vault, Suricata & Zone services sÃ©curisÃ©s \\
Management & 192.168.30.0/24 & Wazuh OVA & Zone administration et SIEM \\
\bottomrule
\end{tabular}
\end{table}

\subsection{Composants rÃ©seau}

Un routeur \textbf{MikroTik CHR} (RouterOS v7) assure le routage inter-VLAN et implÃ©mente les rÃ¨gles de pare-feu (firewall) :
\begin{itemize}
  \item Autorisation explicite du port 8883 (MQTT/TLS) de la zone IoT vers la zone Services.
  \item Blocage de tout trafic direct IoT $\to$ Management.
  \item NAT masquerade pour l'accÃ¨s Internet (mises Ã  jour, time sync).
\end{itemize}

\begin{figure}[H]
  \centering
  \includegraphics[width=\textwidth]{figures/gns3-topology.png}
  \caption{Topologie rÃ©seau dans GNS3}
  \label{fig:gns3_topology}
\end{figure}

\section{Composants applicatifs}

\subsection{Broker MQTT â€” Mosquitto}

Eclipse Mosquitto v2.0 est dÃ©ployÃ© en conteneur Docker sur la zone Services (192.168.20.10). Il est configurÃ© avec :
\begin{itemize}
  \item TLS 1.3 strict (port 8883 uniquement, port 1883 dÃ©sactivÃ©).
  \item mTLS obligatoire (\texttt{require\_certificate true}).
  \item ACL basÃ©e sur le \textbf{Common Name} du certificat client.
\end{itemize}

\subsection{PKI â€” HashiCorp Vault}

Vault v1.15 est dÃ©ployÃ© sur 192.168.20.20. Il hÃ©berge :
\begin{itemize}
  \item Un moteur PKI racine (\texttt{pki}) avec une CA hors-ligne simulÃ©e.
  \item Un moteur PKI intermÃ©diaire (\texttt{pki\_int}) Ã©mettant les certificats de services et de dispositifs.
  \item Des politiques Vault contrÃ´lant l'accÃ¨s Ã  l'API d'Ã©mission de certificats.
\end{itemize}

\subsection{Fleet Simulator}

Le simulateur de flotte est un conteneur Python (192.168.10.X) rejouant le dataset CSV de 76\,000 Ã©vÃ©nements. Chaque dispositif simulÃ© possÃ¨de un certificat unique Ã©mis par Vault.

\subsection{Suricata IDS}

Suricata v7.0 est dÃ©ployÃ© en mode \textit{AF\_PACKET} sur le segment Services, avec une copie du trafic rÃ©seau via un switch GNS3 en mode hub.

\subsection{Wazuh SIEM}

Wazuh v4.7 est dÃ©ployÃ© sous forme d'OVA VMware sur 192.168.30.128. Il ingÃ¨re :
\begin{itemize}
  \item Les alertes Suricata (\texttt{/var/log/suricata/eve.json}).
  \item Les logs du broker Mosquitto.
  \item Les journaux systÃ¨me des conteneurs.
\end{itemize}

\section{Flux de donnÃ©es E2E sÃ©curisÃ©}

Le flux de communication de bout en bout suit les Ã©tapes suivantes :

\begin{enumerate}
  \item Le dispositif IoT demande un certificat Ã  Vault via l'API REST (authentification par token).
  \item Vault Ã©met un certificat X.509 signÃ© par la CA intermÃ©diaire, valide 24 heures.
  \item Le dispositif Ã©tablit une connexion TLS 1.3 avec Mosquitto (port 8883), en prÃ©sentant son certificat.
  \item Mosquitto valide la chaÃ®ne de certification et autorise la connexion si le CN correspond Ã  la politique ACL.
  \item Les messages MQTT sont publiÃ©s sur les topics autorisÃ©s et transitÃ©s vers les subscribers.
  \item Suricata inspecte le trafic rÃ©seau et gÃ©nÃ¨re des alertes JSON dans \texttt{eve.json}.
  \item Wazuh ingÃ¨re les alertes Suricata et les corrÃ¨le avec d'autres Ã©vÃ©nements.
  \item Le pipeline ML analyse les mÃ©triques de communication pour dÃ©tecter des anomalies.
\end{enumerate}

\section{Choix technologiques}

Les principaux choix techniques ont Ã©tÃ© motivÃ©s par les critÃ¨res suivants :

\begin{table}[H]
\centering
\caption{Justification des choix technologiques}
\label{tab:choix_tech}
\begin{tabular}{llp{6cm}}
\toprule
\textbf{Composant} & \textbf{Technologie} & \textbf{Justification} \\
\midrule
Simulation rÃ©seau & GNS3 & Standard acadÃ©mique, support Docker natif, reproductible \\
Routeur & MikroTik CHR & UtilisÃ© en production chez TT, RouterOS complet \\
Broker MQTT & Mosquitto & Open-source, TLS 1.3 natif, ACL avancÃ©es, communautÃ© active \\
PKI & HashiCorp Vault & API programmatique, rotation automatique, open-source \\
IDS & Suricata & Support MQTT natif, multi-thread, rÃ¨gles compatibles Snort \\
SIEM & Wazuh & OpenSearch intÃ©grÃ©, agents lÃ©gers, gratuit et open-source \\
ML & scikit-learn & Large Ã©cosystÃ¨me, IsolationForest + RF disponibles \\
\bottomrule
\end{tabular}
\end{table}
