\chapter{Infrastructure PKI et HashiCorp Vault}

\section{Conception de la hiÃ©rarchie de certification}

\subsection{Principes de conception}

L'infrastructure PKI adoptÃ©e suit une hiÃ©rarchie Ã  \textbf{deux niveaux} : une CA racine et une CA intermÃ©diaire. Ce schÃ©ma est recommandÃ© par le NIST (SP 800-57) pour plusieurs raisons :

\begin{itemize}
  \item La CA racine peut rester hors-ligne (\textit{offline CA}), rÃ©duisant sa surface d'attaque.
  \item La CA intermÃ©diaire, exposÃ©e aux systÃ¨mes applicatifs, peut Ãªtre rÃ©voquÃ©e et remplacÃ©e sans invalider toute la chaÃ®ne.
  \item Les politiques de certification (contraintes de nommage, durÃ©es de vie) sont dÃ©finies discriminÃ©ment par niveau.
\end{itemize}

\begin{figure}[H]
  \centering
  \includegraphics[width=0.7\textwidth]{figures/vault-pki-hierarchy.png}
  \caption{HiÃ©rarchie PKI avec HashiCorp Vault}
  \label{fig:vault_pki}
\end{figure}

\subsection{ParamÃ¨tres cryptographiques}

\begin{table}[H]
\centering
\caption{ParamÃ¨tres cryptographiques de la PKI}
\label{tab:pki_params}
\begin{tabular}{lll}
\toprule
\textbf{ParamÃ¨tre} & \textbf{CA Racine} & \textbf{CA IntermÃ©diaire} \\
\midrule
Algorithme de clÃ© & RSA-4096 & RSA-2048 \\
Algorithme de signature & SHA-256 & SHA-256 \\
DurÃ©e de validitÃ© & 10 ans & 5 ans \\
Profil de certificat & CA basic constraint & CA basic constraint \\
\bottomrule
\end{tabular}
\end{table}

Pour les certificats \textbf{dispositifs et services}, les paramÃ¨tres sont :
\begin{itemize}
  \item \textbf{Algorithme} : RSA-2048 / SHA-256.
  \item \textbf{ValiditÃ©} : 24 heures (dispositifs IoT â€” renouvellement automatique via script).
  \item \textbf{SAN (Subject Alternative Name)} : Inclut l'IP de la machine et le nom DNS.
  \item \textbf{Key Usage} : \texttt{digitalSignature, keyEncipherment}.
  \item \textbf{Extended Key Usage} : \texttt{clientAuth} (dispositifs), \texttt{serverAuth} (Mosquitto).
\end{itemize}

\section{DÃ©ploiement de HashiCorp Vault}

\subsection{Configuration initiale}

Vault est dÃ©ployÃ© dans un conteneur Docker basÃ© sur l'image officielle \texttt{hashicorp/vault:1.15}. Il est configurÃ© en mode \textbf{development} pour ce prototype, avec un backend de stockage en mÃ©moire. Pour un dÃ©ploiement production, un backend Consul ou Raft (intÃ©grÃ©) serait recommandÃ©.

\begin{lstlisting}[language=bash, caption=Initialisation de Vault et activation du moteur PKI]
# Initialisation et rÃ©cupÃ©ration des clÃ©s unseal
vault operator init -key-shares=3 -key-threshold=2

# DÃ©verrouillage (unseal)
vault operator unseal <key-1>
vault operator unseal <key-2>

# Authentification avec le root token
vault login <root-token>

# Activation du moteur PKI racine
vault secrets enable -path=pki pki
vault secrets tune -max-lease-ttl=87600h pki
\end{lstlisting}

\subsection{CrÃ©ation de la CA racine}

\begin{lstlisting}[language=bash, caption=GÃ©nÃ©ration de la CA racine dans Vault]
vault write -field=certificate pki/root/generate/internal \
    common_name="PFE-IoT Root CA" \
    issuer_name="root-2024" \
    ttl=87600h \
    key_type=rsa \
    key_bits=4096 > /pki/certs/ca/root-ca.crt
\end{lstlisting}

\subsection{CrÃ©ation de la CA intermÃ©diaire}

\begin{lstlisting}[language=bash, caption=GÃ©nÃ©ration et signature de la CA intermÃ©diaire]
# Activation du moteur PKI intermÃ©diaire
vault secrets enable -path=pki_int pki
vault secrets tune -max-lease-ttl=43800h pki_int

# GÃ©nÃ©ration du CSR intermÃ©diaire
vault write -format=json pki_int/intermediate/generate/internal \
    common_name="PFE-IoT Intermediate CA" | \
    jq -r '.data.csr' > /tmp/pki_int.csr

# Signature par la CA racine
vault write -format=json pki/root/sign-intermediate \
    issuer_ref="root-2024" \
    csr=@/tmp/pki_int.csr \
    format=pem_bundle \
    ttl=43800h | \
    jq -r '.data.certificate' > /tmp/pki_int.crt

# Import du certificat signÃ© dans le moteur intermÃ©diaire
vault write pki_int/intermediate/set-signed \
    certificate=@/tmp/pki_int.crt
\end{lstlisting}

\subsection{DÃ©finition des rÃ´les d'Ã©mission}

Deux rÃ´les sont dÃ©finis pour contrÃ´ler les types de certificats Ã©missibles :

\begin{lstlisting}[language=bash, caption=RÃ´les Vault pour services et dispositifs IoT]
# RÃ´le pour les services (Mosquitto, etc.)
vault write pki_int/roles/services \
    allowed_domains="pfe-iot.local" \
    allow_subdomains=true \
    allow_ip_sans=true \
    key_type=rsa key_bits=2048 \
    max_ttl=8760h

# RÃ´le pour les dispositifs IoT (durÃ©e courte)
vault write pki_int/roles/iot-devices \
    allowed_domains="devices.pfe-iot.local" \
    allow_subdomains=true \
    allow_ip_sans=true \
    key_type=rsa key_bits=2048 \
    max_ttl=24h
\end{lstlisting}

\section{Ã‰mission automatique des certificats de dispositifs}

Le script \texttt{generate\_device\_certs.sh} automatise l'Ã©mission d'un certificat par dispositif simulÃ© :

\begin{lstlisting}[language=bash, caption=Ã‰mission d'un certificat de dispositif via l'API Vault]
#!/bin/bash
DEVICE_ID=$1
VAULT_ADDR="http://192.168.20.20:8200"
VAULT_TOKEN="${VAULT_TOKEN}"

# Appel API Vault pour Ã©mettre le certificat
curl -s -X POST \
  "${VAULT_ADDR}/v1/pki_int/issue/iot-devices" \
  -H "X-Vault-Token: ${VAULT_TOKEN}" \
  -d "{\"common_name\": \"${DEVICE_ID}.devices.pfe-iot.local\", \"ttl\": \"24h\"}" | \
  python3 -c "
import sys, json
data = json.load(sys.stdin)['data']
open(f'certs/{DEVICE_ID}.crt', 'w').write(data['certificate'])
open(f'certs/{DEVICE_ID}.key', 'w').write(data['private_key'])
open(f'certs/ca-chain.crt', 'w').write(data['ca_chain'][0])
"
\end{lstlisting}

\section{Validation de la PKI}

La chaÃ®ne de certification est validÃ©e avec \texttt{openssl verify} :

\begin{lstlisting}[language=bash, caption=Validation de la chaÃ®ne de certification]
# VÃ©rification de la chaÃ®ne complÃ¨te
openssl verify -CAfile pki/certs/ca/root-ca.crt \
               -untrusted pki/certs/ca/intermediate-ca.crt \
               pki/certs/devices/device-001.crt

# RÃ©sultat attendu :
# pki/certs/devices/device-001.crt: OK

# Inspection du certificat
openssl x509 -in pki/certs/devices/device-001.crt \
             -noout -text | grep -A5 "Subject:"
\end{lstlisting}

\begin{tcolorbox}[colback=green!5, colframe=green!50!black, title=RÃ©sultat PKI]
\textbf{50 certificats de dispositifs} Ã©mis avec succÃ¨s via l'API Vault. ChaÃ®ne de confiance validÃ©e : dispositif $\to$ CA intermÃ©diaire $\to$ CA racine. DurÃ©e d'Ã©mission moyenne : \textbf{87 ms} par certificat via API REST.
\end{tcolorbox}
