\chapter{Réalisation}
\label{chap:realisation}

\section{Introduction}
\label{sec:re-intro}

Ce chapitre présente la mise en \oe{}uvre concrète de l'architecture conçue précédemment. Nous décrivons successivement~: l'environnement de développement, l'architecture globale et la topologie réseau, la PKI Vault, le broker MQTT en TLS/mTLS, les scénarios d'attaque retenus avec leurs contre-mesures, le pipeline d'apprentissage automatique (présenté \emph{après} la couche de défense statique pour respecter une logique de défense en profondeur), puis les captures d'écran des interfaces clés (Vault UI, Wazuh, GNS3).

\section{Environnement de développement}
\label{sec:re-env}

Le laboratoire est entièrement reproductible sur une station Windows~10/11 (16~Go de RAM minimum). Les principaux composants logiciels sont résumés dans le tableau~\ref{tab:env}.

\begin{table}[!htbp]
\centering
\caption{Composants de l'environnement de développement}
\label{tab:env}
\small
\begin{tabularx}{\linewidth}{|l|l|X|}
\hline
\textbf{Composant} & \textbf{Version} & \textbf{Rôle} \\
\hline
GNS3 & 2.2.x & Émulation de la topologie réseau \\ \hline
Docker Engine & 24.x & Conteneurs services et clients IoT \\ \hline
HashiCorp Vault & 1.15 & PKI à deux niveaux et secrets engine \\ \hline
Eclipse Mosquitto & 2.0 & Broker MQTT avec TLS~1.3 + mTLS \\ \hline
Suricata & 6.0 & IDS réseau, parser MQTT \\ \hline
Wazuh & 4.7 & SIEM, agrégation et corrélation \\ \hline
pfSense & 2.7 & Pare-feu inter-VLAN \\ \hline
MikroTik RouterOS & 7.x & Routage / NAT \\ \hline
Python & 3.11 & Simulateur de flotte, ML \\ \hline
scikit-learn & 1.4 & Algorithmes ML \\ \hline
\end{tabularx}
\end{table}

Ces composants ne fonctionnent pas de manière indépendante~: ils s'articulent en une \textbf{architecture de défense en profondeur} où chaque couche renforce les autres. La section suivante décrit cette organisation globale et le flux de données de bout en bout, avant d'entrer dans le détail de chaque brique.

\section{Architecture globale et topologie réseau}
\label{sec:re-archi}

\subsection{Architecture de déploiement}

L'architecture de déploiement est la traduction concrète, sur la machine hôte, des briques conceptuelles décrites au chapitre~\ref{chap:contexte-general} (figure~\ref{fig:archi-vue-intro}). Elle est présentée en deux parties pour en faciliter la lecture~: la couche réseau et les stacks producteurs de données (Partie~1), puis les stacks consommateurs et d'analyse (Partie~2).

\begin{infobox}{Différence entre la figure~\ref{fig:archi-vue-intro} (Ch.~1) et les figures~\ref{fig:archi-p1}--\ref{fig:archi-p2} (Ch.~4)}
La figure~\ref{fig:archi-vue-intro} est une vue \textbf{conceptuelle}~: elle présente les zones réseau logiques (IoT, DMZ, PKI, SIEM, Analyse), les flux de données et les technologies retenues. Elle répond à la question \emph{«~quoi~?~»}.

Les figures~\ref{fig:archi-p1} et~\ref{fig:archi-p2} constituent une vue \textbf{d'implémentation}~: elles matérialisent la réalisation concrète sur la machine hôte (Intel i3-1215U / 16~Go RAM, Windows~11) en détaillant~:
\begin{itemize}[leftmargin=*]
  \item les stacks Docker dans la VM GNS3 (\emph{IoT Simulée}, \emph{Broker/Gateway}, \emph{PKI}, \emph{SIEM}, \emph{ML/Analyse})~;
  \item les ports d'écoute réels (8883 MQTT/TLS, 8200 Vault, 9200 OpenSearch, 5601 Dashboard, 1514/1515 Wazuh)~;
  \item les relations inter-services~: port miroir MikroTik→Suricata, EVE JSON→Wazuh, API REST→Vault.
\end{itemize}
Elles répondent à la question \emph{«~comment~?~»} et constituent la référence de reproduction de l'environnement.
\end{infobox}

\paragraph{Partie 1 — Infrastructure réseau, couche IoT et couche Broker/IDS.}
La figure~\ref{fig:archi-p1} montre les deux niveaux d'exécution du laboratoire~: (i)~la \textbf{VM GNS3} (VMware Workstation, 4~Go RAM) héberge pfSense (firewall/NAT, quatre VLANs) et MikroTik CHR (switch L3, port miroir vers Suricata)~; (ii)~le \textbf{Docker Engine} exécute le simulateur de flotte (50 threads, 76~000~événements CSV, VLAN~10) et Mosquitto (TLS~1.3 + mTLS + ACL deny-by-default, port~8883, VLAN~20 DMZ).

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{diag-composants-part1.png}
\caption{Architecture de déploiement --- Partie~1~: infrastructure réseau (pfSense, MikroTik), stack IoT simulée et stack Broker/IDS (Mosquitto, Suricata)}
\label{fig:archi-p1}
\end{figure}
\FloatBarrier

\paragraph{Partie 2 — Stack PKI, SIEM et ML/Analyse + flux de données.}
La figure~\ref{fig:archi-p2} détaille les stacks de traitement backend~: \textbf{Vault~1.15} (PKI Engine, CA racine RSA-4096 offline + CA intermédiaire active, port~8200)~; \textbf{Wazuh~4.7} (Manager + Indexer OpenSearch + Dashboard, ports 1514/9200/5601) qui reçoit les alertes EVE~JSON de Suricata via Filebeat et les logs Mosquitto via Syslog~; le \textbf{moteur ML} (IsolationForest + RandomForest, 12~features réseau) alimenté par les sorties Suricata et Wazuh.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{diag-composants-part2.png}
\caption{Architecture de déploiement --- Partie~2~: stack PKI (Vault), stack SIEM (Wazuh) et stack ML/Analyse + interfaces de données}
\label{fig:archi-p2}
\end{figure}
\FloatBarrier

\subsection{Topologie réseau}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{diag-topologie-reseau.png}
\caption{Topologie réseau détaillée~: VLANs, plan d'adressage IP et politique de filtrage}
\label{fig:topo}
\end{figure}
\FloatBarrier

La figure~\ref{fig:topo} détaille la topologie réseau effective déployée dans GNS3. Quatre VLANs sont isolés~: le VLAN \textbf{IoT} (\texttt{192.168.10.0/24}) concentre les 50 dispositifs simulés~; le VLAN \textbf{Services/DMZ} (\texttt{192.168.20.0/24}) héberge le broker Mosquitto et les passerelles~; le VLAN \textbf{PKI} (\texttt{192.168.30.0/24}) isole Vault dont l'API REST est accessible uniquement depuis les zones autorisées~; le VLAN \textbf{SIEM/Monitoring} (\texttt{192.168.40.0/24}) regroupe Suricata, Wazuh et OpenSearch. pfSense applique une politique \emph{deny-by-default} à tous les flux inter-VLAN et n'autorise que les flux explicitement définis dans les règles (MQTT/8883, Vault API/8200, Syslog/1514). Le port miroir MikroTik sur le segment DMZ permet à Suricata d'analyser passivement la totalité du trafic MQTT sans être dans le chemin de données.

Le flux complet est décomposé en cinq phases présentées séparément (figures~\ref{fig:flux-p1}--\ref{fig:flux-p5}) afin d'en améliorer la lisibilité.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.85\linewidth,height=0.32\textheight,keepaspectratio]{diag-flux-phase1.png}
\caption{Flux E2E --- Phase 1 : provisionnement de l'identité}
\label{fig:flux-p1}
\end{figure}
\FloatBarrier

Cette phase initiale décrit la demande d'un certificat client par le dispositif auprès de Vault, depuis l'authentification AppRole jusqu'à la signature par la CA intermédiaire et la livraison sécurisée du couple clé/certificat.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.95\linewidth,height=0.40\textheight,keepaspectratio]{diag-flux-phase2.png}
\caption{Flux E2E --- Phase 2 : établissement de la connexion TLS~1.3 mTLS}
\label{fig:flux-p2}
\end{figure}
\FloatBarrier

Le schéma détaille les échanges du \emph{handshake} TLS~1.3 mutuel entre le dispositif et le broker, en mettant en évidence la vérification croisée des certificats et la dérivation des clés de session.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.85\linewidth,height=0.30\textheight,keepaspectratio]{diag-flux-phase3.png}
\caption{Flux E2E --- Phase 3 : publication des données IoT}
\label{fig:flux-p3}
\end{figure}
\FloatBarrier

Une fois la session sécurisée établie, le dispositif publie ses messages MQTT sur ses topics autorisés~; le broker applique alors les ACL liées au \emph{Common Name} avant toute redistribution aux abonnés.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.92\linewidth,height=0.38\textheight,keepaspectratio]{diag-flux-phase4.png}
\caption{Flux E2E --- Phase 4 : détection et supervision}
\label{fig:flux-p4}
\end{figure}
\FloatBarrier

Cette phase illustre la chaîne d'observation~: les flux sont analysés par Suricata, les événements transmis à Wazuh pour corrélation, et les alertes restituées à l'analyste via le tableau de bord.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.80\linewidth,height=0.30\textheight,keepaspectratio]{diag-flux-phase5.png}
\caption{Flux E2E~--- Phase~5~: rotation automatique des certificats}
\label{fig:flux-p5}
\end{figure}
\FloatBarrier

La dernière phase montre comment Vault déclenche, à l'approche de l'expiration ou suite à une révocation, la régénération d'un certificat et son redéploiement transparent sur le dispositif sans interruption de service. La rotation est automatisée par un agent de renouvellement côté dispositif (minuteur basé sur le TTL reçu lors de l'émission), qui relance la séquence de la phase~1 avant expiration, garantissant une continuité de service sans intervention manuelle.
\FloatBarrier

\section{Infrastructure à clés publiques (Vault)}
\label{sec:re-pki}

\subsection{Hiérarchie}

La PKI suit le modèle à deux niveaux préconisé par le NIST~\cite{nist_sp800213}~: une CA racine \emph{offline} (générée hors ligne, conservée chiffrée) signe une unique CA intermédiaire en ligne, gérée par Vault~\cite{vault_pki}. La figure~\ref{fig:pki-hier} illustre cette hiérarchie.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.85\linewidth]{diag-pki-hierarchy.png}
\caption{Hiérarchie de la PKI à deux niveaux (CA racine \emph{offline} + CA intermédiaire Vault)}
\label{fig:pki-hier}
\end{figure}

Le diagramme distingue les trois niveaux de confiance de la hiérarchie~: (i)~la \textbf{CA racine} (RSA-4096, self-signed, TTL~10~ans) est conservée hors ligne et ne signe que les CA intermédiaires~; sa clé privée n'est jamais exposée dans la VM GNS3~; (ii)~la \textbf{CA intermédiaire} (RSA-2048, TTL~5~ans) est embarquée dans le \emph{secrets engine PKI} de Vault et signe les certificats opérationnels à la demande via API REST~; (iii)~les \textbf{certificats finaux} sont émis selon deux rôles distincts~: \texttt{role-server} (TTL~90~jours, usage \texttt{serverAuth}, pour Mosquitto et Vault), et \texttt{role-device} (TTL~30~jours, usage \texttt{clientAuth}, CN contrôlé au pattern \texttt{dev-XXX.iot.lab}). La séparation des rôles empêche qu'un certificat de service soit utilisé pour s'authentifier comme dispositif, et inversement.

\subsection{Rôles Vault et émission}

Deux rôles PKI ont été définis~: \texttt{role-server} (TTL 90~jours, usage \emph{server\_auth}) et \texttt{role-device} (TTL 30~jours, usage \emph{client\_auth}, \emph{Common Name} contraint au pattern \texttt{dev-XXX.iot.lab}). L'émission d'un certificat client se réduit à un appel HTTP authentifié par jeton AppRole.

\begin{lstlisting}[language=bash, caption={Émission d'un certificat client via Vault}]
vault write -format=json pki_int/issue/role-device \
    common_name="dev-042.iot.lab" \
    ttl="720h"
\end{lstlisting}

Le choix des TTL résulte d'une analyse du compromis \textbf{sécurité / coût opérationnel}~: un TTL court réduit la fenêtre d'exposition en cas de vol de clé privée, mais impose une rotation fréquente, source de charge et d'erreur. Pour les services (broker, Vault), un TTL de 90~jours équilibre la stabilité des configurations et la rotation régulière~; pour les dispositifs, un TTL de 30~jours est préférable car ces objets ont un cycle de vie moins maîtrisé et la rotation est automatisée par Vault, rendant son surcoût négligeable. Ce choix est cohérent avec les recommandations du NIST~\cite{nist_sp800213} qui préconise des TTL inférieurs à 90~jours pour les certificats d'entité terminale dans les environnements IoT.

La PKI étant opérationnelle, chaque composant~-- dispositif comme service~-- dispose d'une identité cryptographique vérifiable par tous les pairs. C'est sur cette fondation que repose la couche transport~: le broker MQTT peut désormais exiger la présentation d'un certificat signé par la CA intermédiaire avant d'accepter toute connexion, comme décrit dans la section suivante.

\section{Broker MQTT en TLS 1.3 + mTLS}
\label{sec:re-mqtt}

Mosquitto est configuré avec~\cite{mosquitto_tls}~: \texttt{require\_certificate true}, \texttt{use\_identity\_as\_username true}, et un fichier d'\acs{ACL} liant chaque \emph{Common Name} à ses topics autorisés. La figure~\ref{fig:tls-handshake} retrace le \emph{handshake} TLS~1.3 mutuel observé entre un dispositif et le broker.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.9\linewidth]{diag-tls-handshake.png}
\caption{\emph{Handshake} TLS~1.3 avec authentification mutuelle (mTLS)}
\label{fig:tls-handshake}
\end{figure}

Le diagramme de séquence se lit selon quatre phases distinctes~:
\begin{enumerate}[leftmargin=*]
  \item \textbf{ClientHello} (dispositif~\textrightarrow~broker)~: le dispositif annonce les suites cryptographiques qu'il supporte et propose les paramètres de partage de clé (ECDHE)~;
  \item \textbf{ServerHello + CertificateVerify} (broker~\textrightarrow~dispositif)~: le broker sélectionne la suite, transmet son certificat X.509 signé par la CA Vault et prouve la possession de sa clé privée~;
  \item \textbf{Certificate (client)} (dispositif~\textrightarrow~broker)~: le dispositif présente son propre certificat (rôle \texttt{role-device}, CN \texttt{dev-XXX.iot.lab})~; le broker vérifie sa validité, son appartenance à la chaîne PKI et l'absence de révocation dans la CRL Vault~;
  \item \textbf{Finished + Application Data}~: les deux parties dérivent les clés de session symétriques (HKDF sur secret ECDHE) et la session chiffrée est établie en \textbf{1-RTT}.
\end{enumerate}
L'authentification mutuelle garantit qu'aucun dispositif sans certificat valide ne peut accéder au broker, et qu'aucun broker frauduleux ne peut se faire passer pour Mosquitto.

L'architecture de confiance est ainsi opérationnelle~: les identités sont gérées par Vault, le canal de communication est chiffré et les accès applicatifs sont restreints par ACL par certificat. Pour valider la robustesse de cet ensemble, six scénarios d'attaque ont été conçus en s'appuyant directement sur les vecteurs identifiés dans l'état de l'art~: interception, usurpation d'identité, déni de service et exfiltration applicative.

\section{Scénarios d'attaque et contre-mesures}
\label{sec:re-attaques}

Six scénarios ont été conçus pour couvrir les principales catégories d'attaques applicables à un broker MQTT. Chaque scénario est défini par un \emph{vecteur}, une \emph{méthode de détection} et une \emph{contre-mesure}. Les scénarios sont rejoués depuis la flotte simulée et corroborés par les alertes Suricata/Wazuh.

\begin{attackbox}[Scénario 1 -- Man-in-the-Middle (TLS désactivé)]
\textbf{Vecteur~:} un dispositif tente de se connecter sur le port 1883 (MQTT en clair) au lieu de 8883 (TLS). Un attaquant en position MITM peut alors capturer puis modifier les messages.\\
\textbf{Détection~:} règle Suricata sur tout flux TCP/1883.\\
\textbf{Contre-mesure~:} pare-feu pfSense bloquant 1883 . broker écoutant uniquement sur 8883 avec \texttt{require\_certificate}.
\end{attackbox}

\begin{attackbox}[Scénario 2 -- Sniffing MQTT en clair]
\textbf{Vecteur~:} interception passive du trafic MQTT non chiffré sur le segment IoT.\\
\textbf{Détection~:} alerte Suricata si un \texttt{PUBLISH} apparaît hors de la session TLS.\\
\textbf{Contre-mesure~:} TLS~1.3 obligatoire . suites cryptographiques restreintes (RFC~9325~\cite{rfc9325}).
\end{attackbox}

\begin{attackbox}[Scénario 3 -- Brute-force d'authentification]
\textbf{Vecteur~:} 500~tentatives de \texttt{CONNECT} en moins de 60~s avec des certificats forgés.\\
\textbf{Détection~:} règle Suricata MQTT (seuil de \texttt{CONNECT} par IP source).\\
\textbf{Contre-mesure~:} certificats clients signés par CA Vault . \emph{rate-limit} pfSense sur le port 8883.
\end{attackbox}

\begin{attackbox}[Scénario 4 -- Exfiltration via topic non autorisé]
\textbf{Vecteur~:} un dispositif compromis tente de \texttt{SUBSCRIBE} à \texttt{tt/admin/\#}.\\
\textbf{Détection~:} log Mosquitto \texttt{ACL denied} . règle Wazuh corrélant N occurrences/15~min.\\
\textbf{Contre-mesure~:} ACL stricte par \emph{Common Name}, granularité au topic.
\end{attackbox}

\begin{attackbox}[Scénario 5 -- Déni de service par flood CONNECT]
\textbf{Vecteur~:} client malveillant ouvrant plusieurs milliers de connexions TLS partielles.\\
\textbf{Détection~:} alertes Suricata sur taux d'ouvertures TLS anormal.\\
\textbf{Contre-mesure~:} \texttt{max\_connections} Mosquitto, \texttt{state-table-limit} pfSense, blacklist dynamique Wazuh.
\end{attackbox}

\begin{attackbox}[Scénario 6 -- Compromission de certificat client]
\textbf{Vecteur~:} certificat \texttt{dev-017} volé puis réutilisé depuis une IP non habituelle.\\
\textbf{Détection~:} module ML (Isolation Forest) signalant l'écart comportemental . corrélation Wazuh IP$\leftrightarrow$identité.\\
\textbf{Contre-mesure~:} révocation Vault (\texttt{vault write pki\_int/revoke serial=...}) et \emph{rotation} immédiate de la CRL.
\end{attackbox}

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.75\linewidth,height=0.66\textheight,keepaspectratio]{diag-suricata-detection.png}
\caption{Chaîne de détection Suricata~\textrightarrow~Wazuh (IDS+SIEM)}
\label{fig:detection}
\end{figure}
\FloatBarrier

Le diagramme décrit le flux d'une alerte depuis sa génération jusqu'à sa restitution à l'analyste. Suricata analyse le trafic en mode passif (port miroir MikroTik) et produit des événements au format \texttt{EVE JSON} dans \texttt{/var/log/suricata/eve.json}. L'agent Filebeat (déployé sur le conteneur Suricata) lit ce fichier et le transmet au serveur Wazuh via le protocole Syslog/1514. Wazuh applique ses décodeurs XML (parsing du champ \texttt{alert.signature}) et ses règles de corrélation (par exemple~: si plus de 5~alertes \texttt{ET MQTT brute-force} en moins de 60~s depuis la même IP source, escalader en alarme critique). OpenSearch indexe les événements et le tableau de bord Wazuh les restitue à l'analyste avec des visualisations filtrable par type d'attaque, VLAN source et période.

\section{Pipeline d'apprentissage automatique}
\label{sec:re-ml}

Les scénarios d'attaque ont mis en évidence une limite inhérente aux approches par règles~: le scénario~S6 (certificat valide réutilisé depuis un contexte inhabituel) n'est détecté que partiellement par Suricata et Wazuh. Le pipeline ML intervient précisément pour combler cette lacune~: il complète la détection par signature en repérant les comportements anormaux que les règles statiques ne peuvent pas anticiper. Il est constitué de cinq étapes (figure~\ref{fig:ml-pipeline})~: collecte du trafic, extraction de features, entraînement, évaluation, intégration dans le SIEM.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.78\linewidth,height=0.58\textheight,keepaspectratio]{diag-ml-pipeline.png}
\caption{Pipeline d'apprentissage automatique (collecte, features, entraînement, évaluation, SIEM)}
\label{fig:ml-pipeline}
\end{figure}

Le pipeline se lit de gauche à droite~: (1)~\textbf{Collecte}~: les captures de trafic (PCAP ou logs Mosquitto) générées lors des six sprints alimentent le jeu de données~; (2)~\textbf{Feature engineering}~: 12~attributs sont extraits par message (inter-arrival time, payload length, topic depth, QoS level, type de commande MQTT, fréquence de reconnexion, etc.)~; (3)~\textbf{Entraînement}~: IsolationForest est entraîné sur le trafic normal uniquement (non supervisé), Random Forest est entraîné sur le jeu étiqueté complet (supervisé)~; (4)~\textbf{Évaluation}~: les métriques F1, précision et rappel sont calculées sur le jeu de test (20~\% du dataset)~; (5)~\textbf{Intégration}~: les scores d'anomalie sont transmis à Wazuh comme événements JSON, permettant leur corrélation avec les alertes Suricata pour une détection multicouche.

\subsection{Données et features}

Le jeu de données utilisé contient 76\,000 enregistrements répartis en huit classes (1 normale + 7 attaques)~\cite{hasan2020securing}. Les figures~\ref{fig:eda-classes}--\ref{fig:eda-corr} présentent l'analyse exploratoire.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.85\linewidth,height=0.38\textheight,keepaspectratio]{eda_classes_distribution.png}
\caption{Analyse exploratoire --- distribution des classes}
\label{fig:eda-classes}
\end{figure}
\FloatBarrier

Ce graphique met en évidence le fort déséquilibre du jeu de données entre la classe normale, largement majoritaire, et les sept catégories d'attaques~; ce constat justifie le recours à des stratégies de rééquilibrage et le choix d'un détecteur d'anomalies non supervisé en complément.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.95\linewidth,height=0.45\textheight,keepaspectratio]{eda_correlation_heatmap.png}
\caption{Analyse exploratoire --- matrice de corrélation des features numériques}
\label{fig:eda-corr}
\end{figure}
\FloatBarrier

La matrice de corrélation révèle les variables fortement liées entre elles, ce qui permet d'écarter les features redondantes et de retenir un sous-ensemble informatif pour l'entraînement des modèles.
\FloatBarrier

\subsection{Modèles}

Deux modèles complémentaires sont entraînés via \emph{scikit-learn}~\cite{scikit_learn}~:

\begin{itemize}[leftmargin=*]
  \item \textbf{Isolation Forest}~\cite{liu2008isolation}~: détection non supervisée d'anomalies sur trafic légitime majoritaire .
  \item \textbf{Random Forest}~\cite{breiman2001random}~: classification supervisée multi-classes des sept attaques.
\end{itemize}

\section{Captures d'écran des interfaces}
\label{sec:re-captures}

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{vault-pki-hierarchy.png}
\caption{Vault UI~: vue de la hiérarchie PKI (CA racine et CA intermédiaire actives)}
\label{fig:cap-vault}
\end{figure}

L'interface Vault confirme la structure à deux niveaux~: la CA racine (\emph{offline}) apparaît avec un statut \emph{expired} volontaire (sa clé privée n'est pas chargée en mémoire), tandis que la CA intermédiaire est marquée \emph{active} et prend en charge l'émission des certificats opérationnels.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{wazuh-dashboard.png}
\caption{Wazuh Dashboard~: alertes corrélées issues des scénarios d'attaque}
\label{fig:cap-wazuh}
\end{figure}

Le tableau de bord Wazuh synthétise les événements collectés lors du rejeu des six scénarios d'attaque. La vue \emph{Top rules} met en évidence les règles les plus déclenchées~; le filtre temporel permet d'isoler les alertes par sprint et de vérifier la corrélation entre les injections du simulateur et les détections effectives.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{gns3-topology.png}
\caption{GNS3~: topologie déployée du laboratoire (VMs et conteneurs Docker visibles)}
\label{fig:cap-gns3}
\end{figure}

La capture GNS3 montre la topologie telle qu'elle est réellement déployée~: pfSense, MikroTik CHR et les conteneurs Docker apparaissent comme des nœuds distints. Les liens entre nœuds représentent des connexions Ethernet virtuelles gachées dans des VLANs séparés. Cette vue confirme la reproductibilité de l'architecture~: l'ensemble du projet GNS3 est versionné et peut être importé sur une autre station disposant de GNS3~2.2.x et VMware Workstation.

\section{Conclusion}
\label{sec:re-conclusion}

Ce chapitre a décrit la mise en \oe{}uvre concrète de l'architecture conçue~: un environnement reproductible sous GNS3 et Docker, une PKI Vault à deux niveaux assurant l'émission et la révocation automatisées, un broker Mosquitto imposant mTLS à toutes les connexions, une flotte de 50 dispositifs simulant 76\,000 messages réalistes, six scénarios d'attaque couvrant les principaux vecteurs IoT, et un pipeline ML complémentaire. Chacune de ces briques répond directement à l'un des objectifs O1--O7 définis au chapitre~\ref{chap:introduction}~: la réalisation valide ainsi la conception dans ses dimensions fonctionnelles et sécuritaires. Il reste à \textbf{mesurer} ces réalisations de façon rigoureuse~: c'est l'objet du chapitre suivant.
