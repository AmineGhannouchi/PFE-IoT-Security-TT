\chapter{Conception}
\label{chap:conception}

\section{Introduction}
\label{sec:co-intro}

Ce chapitre a pour objectif de traduire, dans le contexte du projet, les enseignements de l'état de l'art en une conception structurée, argumentée et directement exploitable pour la phase de réalisation. Le diagnostic qui l'ouvre ne reprend donc pas l'analyse théorique précédente~: il en propose une lecture opérationnelle afin d'identifier les lacunes à traiter dans l'architecture cible. Le chapitre présente ensuite la spécification des besoins fonctionnels et non fonctionnels, puis une vue d'ensemble de la solution proposée. Il expose enfin les principaux artefacts de pilotage et de modélisation du projet~: la planification des sprints, le backlog produit, les diagrammes UML et la planification temporelle, qui constituent la référence de travail pour la mise en \oe{}uvre.

\section{Diagnostic de l'existant}
\label{sec:analyse-existant}

\subsection{État des lieux de la sécurité IoT dans les déploiements opérateur}

L'analyse conduite en amont de ce projet, combinant l'étude des pratiques observées dans des déploiements IoT typiques et les enseignements publiés par l'ENISA~\cite{enisa_iot} et le NIST~\cite{nist_sp800213}, met en évidence plusieurs lacunes de sécurité récurrentes dans ce type d'environnement~:

\begin{itemize}[leftmargin=*]
	\item \textbf{Transport non systématiquement chiffré}~: le protocole MQTT est fréquemment déployé sur le port 1883 (texte clair), sans TLS, par souci de simplicité ou de performance perçue. Un attaquant positionné sur le segment réseau peut alors capturer l'intégralité des messages publiés, y compris les données métier sensibles~;
	\item \textbf{Authentification unidirectionnelle ou absente}~: le broker ne vérifie généralement pas le certificat du client. Les dispositifs s'authentifient souvent par un simple couple identifiant/mot de passe, voire sans aucun mécanisme d'authentification, rendant toute usurpation d'identité triviale~;
	\item \textbf{Absence de PKI dédiée}~: lorsque des certificats existent, ils sont fréquemment auto-signés, non révocables automatiquement et gérés de façon ad hoc, sans cycle de vie défini ni procédure de révocation d'urgence en cas de compromission~;
	\item \textbf{Contrôle d'accès applicatif insuffisant}~: les listes de contrôle d'accès aux topics MQTT sont rarement mises en œuvre, permettant à tout dispositif connecté d'accéder à l'ensemble des canaux de publication et de souscription, indépendamment de son rôle légitime~;
	\item \textbf{Supervision fragmentée}~: les outils de monitoring réseau et applicatif ne communiquent pas entre eux, rendant difficile la corrélation d'événements de sécurité. La détection d'une attaque nécessite une analyse manuelle croisée de plusieurs sources de journaux hétérogènes~;
	\item \textbf{Absence de détection comportementale}~: les mécanismes de détection d'intrusion existants se limitent aux signatures connues. Ils ne permettent pas de détecter des anomalies comportementales subtiles telles que l'utilisation d'un certificat valide depuis un contexte inhabituel, une dérive de fréquence de publication ou une exfiltration lente via des topics légitimes.
\end{itemize}

\subsection{Synthèse du diagnostic}

Le tableau~\ref{tab:diagnostic} synthétise ce diagnostic en mettant en regard chaque lacune identifiée avec la brique de sécurité retenue dans la solution, établissant ainsi le lien direct entre le contexte opérationnel observé et les choix de conception du projet.

\begin{table}[!htbp]
\centering
\caption{Diagnostic des lacunes et briques de réponse correspondantes}
\label{tab:diagnostic}
\small
\begin{tabularx}{\linewidth}{|>{\raggedright\arraybackslash}X|>{\raggedright\arraybackslash}X|}
\hline
\textbf{Lacune identifiée} & \textbf{Brique de réponse retenue} \\
\hline
Flux MQTT en clair, sans chiffrement de bout en bout & Activation obligatoire de TLS~1.3 sur Mosquitto (port~8883) \\ \hline
Authentification client absente ou basée sur mot de passe faible & Certificat X.509 client obligatoire, vérifié par le broker en mTLS \\ \hline
Gestion manuelle et non automatisée des certificats & PKI HashiCorp Vault à deux niveaux avec émission et révocation via API \\ \hline
Accès applicatifs non restreints par dispositif & ACL Mosquitto fondées sur le \emph{Common Name} du certificat client \\ \hline
Supervision réseau et applicative déconnectées & Intégration de Suricata au SIEM Wazuh pour la corrélation \\ \hline
Absence de détection comportementale & Module ML fondé sur IsolationForest et Random Forest \\ \hline
\end{tabularx}
\end{table}

Ce diagnostic structuré constitue la justification directe des choix techniques opérés dans ce projet~: chaque composant de la solution répond à une lacune précisément identifiée. Cette correspondance garantit la pertinence de l'architecture proposée au-delà d'une simple accumulation d'outils technologiques.

\section{Spécification des besoins}
\label{sec:co-besoins}

\subsection{Besoins fonctionnels (BF)}

\begin{table}[!htbp]
\centering
\caption{Besoins fonctionnels}
\label{tab:bf}
\begin{tabularx}{\linewidth}{|c|>{\raggedright\arraybackslash}X|}
\hline
\textbf{ID} & \textbf{Description} \\
\hline
\acs{BF-1} & Émettre, renouveler et révoquer automatiquement des certificats X.509 pour services et dispositifs. \\ \hline
\acs{BF-2} & Imposer le chiffrement TLS~1.3 et l'authentification mutuelle (mTLS) sur le broker MQTT. \\ \hline
\acs{BF-3} & Restreindre l'accès aux topics MQTT par dispositif via des listes de contrôle d'accès. \\ \hline
\acs{BF-4} & Simuler une flotte hétérogène de 50 dispositifs IoT générant un trafic réaliste. \\ \hline
\acs{BF-5} & Détecter les attaques réseau et applicatives par règles IDS et corréler les alertes dans un SIEM. \\ \hline
\acs{BF-6} & Détecter les anomalies non couvertes par les règles via un modèle d'apprentissage automatique. \\ \hline
\end{tabularx}
\end{table}

\subsection{Besoins non fonctionnels (BNF)}

\begin{table}[!htbp]
\centering
\caption{Besoins non fonctionnels}
\label{tab:bnf}
\begin{tabularx}{\linewidth}{|c|>{\raggedright\arraybackslash}X|}
\hline
\textbf{ID} & \textbf{Description} \\
\hline
\acs{BNF-1} & \textbf{Sécurité}~: aucun flux IoT en clair . rotation des secrets . séparation CA racine / intermédiaire. \\ \hline
\acs{BNF-2} & \textbf{Performance}~: latence handshake mTLS \(<50\) ms . débit broker \(>500\) msg/s par client. \\ \hline
\acs{BNF-3} & \textbf{Reproductibilité}~: l'environnement complet doit être réinstallable à partir des scripts fournis. \\ \hline
\acs{BNF-4} & \textbf{Scalabilité}~: l'architecture doit accepter sans refonte au moins 200 dispositifs. \\ \hline
\acs{BNF-5} & \textbf{Maintenabilité}~: configurations versionnées, modules indépendants, journalisation centralisée. \\ \hline
\acs{BNF-6} & \textbf{Observabilité}~: tableau de bord Wazuh accessible aux analystes en moins de 3 clics. \\ \hline
\end{tabularx}
\end{table}

Ces besoins définissent le \emph{quoi} de la solution~: les fonctionnalités attendues et les contraintes opérationnelles à respecter. Le diagramme de cas d'utilisation est présenté en trois parties, une par acteur de la plateforme.

\subsection{Diagramme de cas d'utilisation}

Trois acteurs principaux interagissent avec la plateforme~: le \textbf{dispositif IoT}, l'\textbf{administrateur sécurité} et l'\textbf{opérateur SOC}. Les relations d'inclusion correspondent à des sous-cas obligatoires, tandis que les relations d'extension représentent des comportements optionnels. Dans ce cadre, la connexion MQTT impose systématiquement l'authentification mTLS et le chiffrement TLS~1.3, alors que l'IDS et le module ML complètent la détection par des mécanismes additionnels de blocage ou d'analyse.

\paragraph{Partie 1 — Dispositif IoT.}
La figure~\ref{fig:uc-p1} détaille les quatre cas d'utilisation du dispositif IoT~: demande de certificat X.509 avec vérification de la chaîne CA, connexion au broker MQTT avec authentification mTLS et chiffrement TLS~1.3, publication de télémétrie et renouvellement automatique du certificat.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.88\linewidth]{diag-uc-part1.png}
\caption{Cas d'utilisation --- Partie~1~: Dispositif IoT (authentification, publication, renouvellement)}
\label{fig:uc-p1}
\end{figure}
\FloatBarrier

\paragraph{Partie 2 — Administrateur sécurité.}
La figure~\ref{fig:uc-p2} montre les quatre cas d'utilisation de l'administrateur~: configuration de la PKI Vault (hiérarchie CA, rôles, TTL), définition des règles ACL Mosquitto, paramétrage des règles IDS Suricata et déploiement des règles de corrélation SIEM Wazuh.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.72\linewidth]{diag-uc-part2.png}
\caption{Cas d'utilisation --- Partie~2~: Administrateur sécurité (PKI, ACL, IDS, SIEM)}
\label{fig:uc-p2}
\end{figure}
\FloatBarrier

\paragraph{Partie 3 — Opérateur SOC.}
La figure~\ref{fig:uc-p3} représente les quatre cas d'utilisation de l'opérateur~: consultation du tableau de bord Wazuh, analyse des alertes IDS, lancement de la détection ML d'anomalies et évaluation des performances de la plateforme.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.72\linewidth]{diag-uc-part3.png}
\caption{Cas d'utilisation --- Partie~3~: Opérateur TT/SOC (dashboard, alertes, ML, performances)}
\label{fig:uc-p3}
\end{figure}
\FloatBarrier

\section{Vue d'ensemble de la solution proposée}
\label{sec:solution-proposee}

Pour répondre à la problématique et atteindre les objectifs ci-dessus, nous proposons une \textbf{architecture de défense en profondeur} composée de six briques complémentaires, intégralement déployée dans un laboratoire simulé sous \acs{GNS3}~\cite{gns3_docs} et Docker~\cite{docker_docs}~:

\begin{enumerate}[leftmargin=*]
	\item Une \textbf{infrastructure réseau segmentée} (VLANs IoT, services, monitoring, management) construite autour d'un pare-feu pfSense et d'un routeur MikroTik~.
	\item Une \textbf{PKI automatisée} HashiCorp Vault à deux niveaux (CA racine hors ligne + CA intermédiaire en ligne), capable d'émettre à la demande des certificats X.509 pour les services et les dispositifs~\cite{vault_pki}~.
	\item Un \textbf{broker MQTT Mosquitto} configuré en \acs{TLS}~1.3 avec authentification mutuelle obligatoire et listes de contrôle d'accès par topic~\cite{mosquitto_tls}~.
	\item Un \textbf{simulateur de flotte} de 50 dispositifs Python rejouant 76\,000 messages issus d'un jeu de données réel, dont sept catégories d'attaques~.
	\item Une \textbf{chaîne de détection} comprenant un IDS Suricata avec règles MQTT personnalisées~\cite{suricata_mqtt} et un SIEM Wazuh~\cite{wazuh_docs} pour la corrélation et la visualisation~.
	\item Un \textbf{pipeline de \acs{ML}} (IsolationForest~\cite{liu2008isolation} et Random Forest~\cite{breiman2001random}) entraîné sur les caractéristiques extraites du trafic, capable de détecter les anomalies non couvertes par les règles statiques.
\end{enumerate}

Le tableau~\ref{tab:mapping-sol} établit la correspondance entre les lacunes diagnostiquées à la section~\ref{sec:analyse-existant}, les briques de la solution et les objectifs spécifiques auxquels elles répondent, assurant ainsi la traçabilité complète entre le diagnostic initial et l'architecture proposée.

\begin{table}[!htbp]
\centering
\caption{Correspondance lacunes -- briques de sécurité -- objectifs spécifiques}
\label{tab:mapping-sol}
\small
\begin{tabularx}{\linewidth}{|>{\raggedright\arraybackslash}X|l|c|}
\hline
\textbf{Brique de sécurité} & \textbf{Section de réalisation} & \textbf{Obj.} \\
\hline
Topologie réseau segmentée (GNS3, pfSense, MikroTik) & §\ref{sec:re-archi} & O1 \\ \hline
PKI Vault à deux niveaux (CA racine + CA intermédiaire) & §\ref{sec:re-pki} & O2 \\ \hline
Broker Mosquitto TLS~1.3 + mTLS + ACL par topic & §\ref{sec:re-mqtt} & O3 \\ \hline
Simulateur de flotte de 50 dispositifs hétérogènes & §\ref{sec:re-attaques} & O4 \\ \hline
IDS Suricata avec règles MQTT dédiées & §\ref{sec:re-attaques} & O5 \\ \hline
SIEM Wazuh (corrélation d'alertes et tableau de bord) & §\ref{sec:re-attaques} & O6 \\ \hline
Module ML (IsolationForest + Random Forest) & §\ref{sec:re-ml} & O7 \\ \hline
\end{tabularx}
\end{table}

La figure~\ref{fig:archi-vue-intro} schématise cette vision globale, qui sera détaillée et justifiée dans les chapitres suivants.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\textwidth]{diag-architecture-generale.png}
\caption{Vue d'ensemble de l'architecture de sécurisation proposée. \textit{Abréviations : GW = passerelle applicative, FW = pare-feu.}}
\label{fig:archi-vue-intro}
\end{figure}

La démarche Scrum retenue au chapitre~\ref{chap:contexte-general} structure la réalisation en six sprints~; la section suivante en détaille la planification et les livrables.

\section{Planification des sprints}
\label{sec:co-sprints}

Six sprints de deux semaines structurent la phase de réalisation. Le tableau~\ref{tab:sprints} en présente les objectifs et les livrables. Cette planification précède intentionnellement le \emph{backlog produit}~: elle fournit la grille de lecture qui permet ensuite d'affecter les \emph{User Stories} aux sprints concernés.

\begin{table}[!htbp]
\centering
\caption{Planification des sprints}
\label{tab:sprints}
\small
\begin{tabularx}{\linewidth}{|l|l|>{\raggedright\arraybackslash}X|}
\hline
\textbf{Sprint} & \textbf{Durée} & \textbf{Objectif principal} \\
\hline
S1 -- Cadrage \& réseau & 2~semaines & Topologie GNS3 segmentée, conteneurs de base, plan d'adressage. \\ \hline
S2 -- PKI Vault & 2~semaines & Vault déployé, CA racine + intermédiaire, rôles PKI, scripts d'émission. \\ \hline
S3 -- Broker mTLS & 2~semaines & Mosquitto en TLS~1.3+mTLS, ACL par certificat, tests d'authentification. \\ \hline
S4 -- Flotte simulée & 2~semaines & 50 clients Python, génération de trafic, rejeu de 76\,000 messages. \\ \hline
S5 -- Détection IDS+SIEM & 2~semaines & Règles Suricata MQTT, intégration Wazuh, dashboards. \\ \hline
S6 -- ML \& bilan & 2~semaines & Modèles IsolationForest et RandomForest, évaluation et rédaction finale. \\ \hline
\end{tabularx}
\end{table}

La figure~\ref{fig:sprints} illustre l'enchaînement des six sprints, leurs dépendances et les jalons de validation associés. On observe que les sprints S1 à S3 forment un bloc séquentiel strict~: la topologie réseau (S1) est un prérequis à l'installation de Vault (S2), elle-même prérequis à la configuration du broker mTLS (S3). À partir de S4, les flux peuvent se paralléliser partiellement~: la flotte simulée et la chaîne de détection IDS+SIEM (S5) peuvent être développées simultanément dès lors que le broker est opérationnel, et le pipeline ML (S6) consomme les données capturées lors de S4--S5.

\begin{figure}[H]
\centering
\includegraphics[width=0.86\linewidth,height=0.16\textheight,keepaspectratio]{diag-scrum-sprints.png}
\caption{Enchaînement des six sprints et leurs dépendances}
\label{fig:sprints}
\end{figure}

\section{Backlog produit}
\label{sec:co-backlog}

Le \emph{backlog produit} regroupe les \emph{User Stories} priorisées selon la méthode \textbf{MoSCoW}. Chaque US est ensuite estimée en \emph{story points}, sur la base d'une échelle de Fibonacci modifiée, puis rattachée à un sprint cible.

\begin{table}[!htbp]
\centering
\caption{Backlog produit (extrait priorisé)}
\label{tab:backlog}
\small
\begin{tabularx}{\linewidth}{|l|l|>{\raggedright\arraybackslash}X|c|c|c|}
\hline
\textbf{ID} & \textbf{Acteur} & \textbf{User Story} & \textbf{Prio.} & \textbf{SP} & \textbf{Sprint} \\
\hline
US-01 & Admin & Déployer une topologie réseau segmentée sous GNS3 & M & 8 & S1 \\ \hline
US-02 & Admin & Définir un plan d'adressage par VLAN & M & 3 & S1 \\ \hline
US-03 & Admin & Démarrer Vault et initialiser une CA racine & M & 5 & S2 \\ \hline
US-04 & Admin & Provisionner une CA intermédiaire et des rôles PKI & M & 8 & S2 \\ \hline
US-05 & Admin & Émettre des certificats serveur/client à la demande & M & 5 & S2 \\ \hline
US-06 & Admin & Configurer Mosquitto en TLS~1.3 + mTLS & M & 8 & S3 \\ \hline
US-07 & Admin & Définir des ACL par identifiant client & M & 5 & S3 \\ \hline
US-08 & Dispositif & Établir une session mTLS et publier sur son topic & M & 5 & S3 \\ \hline
US-09 & Admin & Simuler 50 dispositifs hétérogènes & M & 8 & S4 \\ \hline
US-10 & Admin & Rejouer un dataset de 76\,000 messages & S & 5 & S4 \\ \hline
US-11 & Analyste & Détecter une connexion non autorisée & M & 5 & S5 \\ \hline
US-12 & Analyste & Détecter un brute-force d'authentification & M & 5 & S5 \\ \hline
US-13 & Analyste & Visualiser les alertes corrélées dans Wazuh & M & 5 & S5 \\ \hline
US-14 & Analyste & Détecter une anomalie via IsolationForest & S & 8 & S6 \\ \hline
US-15 & Analyste & Classifier les attaques via Random Forest & S & 8 & S6 \\ \hline
US-16 & Analyste & Comparer ML vs règles IDS & C & 5 & S6 \\ \hline
\end{tabularx}
\end{table}

Soit un total estimé à \(96\) story points sur 6~sprints (\(\sim 16\) SP/sprint), conforme à la capacité d'une équipe solo avec encadrement.

Le backlog ainsi constitué traduit les objectifs en livrables concrets, chacun affecté à un sprint précis. Pour compléter cette conception, il est nécessaire de modéliser les interactions entre les composants~: les diagrammes UML présentés dans la section suivante formalisent le simulateur de flotte, le cycle d'émission des certificats et le comportement d'un dispositif, fournissant une référence de conception pour la phase de réalisation.

\section{Modélisation UML}
\label{sec:co-uml}

\subsection{Diagramme de classes du simulateur}

Le simulateur de flotte (\texttt{fleet\_sim.py}) est structuré en cinq classes réparties en deux groupes logiques. Le premier regroupe le cœur d'orchestration de la simulation~: \texttt{FleetOrchestrator}, \texttt{DeviceSimulator} et \texttt{DeviceStats}. Le second rassemble les composants de configuration et de gestion PKI~: \texttt{BrokerConfig}, \texttt{CertificateManager} et \texttt{CertBundle}. Le diagramme est présenté en deux parties afin d'en faciliter la lecture.

\paragraph{Partie 1 — Orchestration de la simulation.}
La figure~\ref{fig:classe-p1} montre la hiérarchie de contrôle~: \texttt{FleetOrchestrator} crée jusqu'à 50 instances \texttt{DeviceSimulator} (composition), chacune maintenant un agrégat \texttt{DeviceStats} pour la collecte des métriques (messages envoyés/échoués, débit, types d'attaques injectées). La méthode \texttt{\_inject\_dos\_pattern()} de \texttt{DeviceSimulator} module dynamiquement le taux d'émission pour simuler les attaques par inondation du jeu de données.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.75\linewidth]{diag-classe-part1.png}
\caption{Diagramme de classes --- Partie~1~: orchestration (\texttt{FleetOrchestrator}, \texttt{DeviceSimulator}, \texttt{DeviceStats})}
\label{fig:classe-p1}
\end{figure}
\FloatBarrier

\paragraph{Partie 2 — Configuration, PKI et dépendances externes.}
La figure~\ref{fig:classe-p2} détaille les composants de support~: \texttt{BrokerConfig} encapsule les paramètres TLS du broker (hôte, port 8883, chemin du CA, QoS)~; \texttt{CertificateManager} interroge l'API Vault pour émettre, vérifier et renouveler les certificats X.509~; \texttt{CertBundle} (dataclass) transporte le triplet certificat/clé privée/chaîne CA. Les dépendances externes (\texttt{paho.mqtt.Client}, \texttt{pandas.DataFrame}) sont représentées en gris pour les distinguer du code du projet.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.95\linewidth]{diag-classe-part2.png}
\caption{Diagramme de classes --- Partie~2~: configuration, PKI et dépendances externes (\texttt{BrokerConfig}, \texttt{CertificateManager}, \texttt{CertBundle})}
\label{fig:classe-p2}
\end{figure}
\FloatBarrier

\subsection{Diagramme de séquence~: émission d'un certificat client}

La séquence est découpée en deux parties~: l'initialisation de la PKI (opération réalisée une seule fois par l'administrateur) et l'émission effective d'un certificat pour chaque dispositif ou service.

\paragraph{Partie 1 — Initialisation de la PKI.}
La figure~\ref{fig:seq-pki-p1} couvre la mise en place de la hiérarchie à deux niveaux~: activation du moteur PKI racine dans Vault (RSA-4096, TTL~10~ans, auto-signé et conservé \emph{offline}), génération d'une CA intermédiaire dont le CSR est signé par la CA racine, puis import du certificat signé dans le \emph{secrets engine} \texttt{pki\_int}. Enfin, le rôle \texttt{iot-devices} est créé pour contraindre les émissions~: TTL maximal de 30~jours, usage \texttt{clientAuth}, CN limité au pattern \texttt{*.iot.lab}.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\linewidth]{diag-pki-seq-part1.png}
\caption{Séquence PKI --- Partie~1~: initialisation (CA racine, CA intermédiaire, rôle)}
\label{fig:seq-pki-p1}
\end{figure}
\FloatBarrier

\paragraph{Partie 2 — Émission par dispositif.}
La figure~\ref{fig:seq-pki-p2} décrit l'opération répétée pour chaque dispositif~: (1)~le script de provisionnement envoie une requête \texttt{POST} à l'API d'émission du rôle \texttt{iot-devices}, avec le CN et le TTL souhaité~; (2)~Vault vérifie la policy AppRole du token présenté~; (3)~Vault génère la paire de clés RSA-2048 et signe le certificat via la CA intermédiaire~; (4)~le bundle (certificat + clé privée + chaîne CA) est retourné en JSON~; (5)~le dispositif écrit les trois fichiers dans son système de fichiers et démarre un minuteur de renouvellement basé sur le TTL reçu (~87~ms de bout en bout).

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.88\linewidth]{diag-pki-seq-part2.png}
\caption{Séquence PKI --- Partie~2~: émission d'un certificat client (\textasciitilde 87~ms)}
\label{fig:seq-pki-p2}
\end{figure}
\FloatBarrier

\subsection{Diagramme d'activité~: cycle de publication mTLS}

Le cycle complet d'un dispositif simulé est décomposé en trois phases successives (figures~\ref{fig:activity-p1}, \ref{fig:activity-p2} et~\ref{fig:activity-p3}).

\paragraph{Phase 1 — Initialisation de la flotte.}
La figure~\ref{fig:activity-p1} montre le démarrage du simulateur~: le dataset CSV (76\,000 événements) et la configuration du broker sont chargés, puis 50 threads sont instanciés en parallèle via un \emph{fork}, un par dispositif simulé.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.72\linewidth]{diag-activity-part1.png}
\caption{Activité --- Phase~1~: initialisation des 50 threads de la flotte}
\label{fig:activity-p1}
\end{figure}
\FloatBarrier

\paragraph{Phase 2 — Provisionnement et connexion mTLS.}
La figure~\ref{fig:activity-p2} détaille les étapes de provisionnement de chaque thread~: chargement du certificat X.509 émis par Vault, validation des fichiers (une clé ou un certificat invalide provoque un arrêt immédiat), configuration du client MQTT en TLS~1.3, puis établissement de la connexion avec \emph{handshake} mutuel. Un échec de connexion déclenche une attente en back-off exponentiel avant nouvelle tentative (maximum 3 essais), évitant une boucle de reconnexion aveugle.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.72\linewidth]{diag-activity-part2.png}
\caption{Activité --- Phase~2~: provisionnement du certificat et handshake mTLS}
\label{fig:activity-p2}
\end{figure}
\FloatBarrier

\paragraph{Phase 3 — Boucle de publication.}
La figure~\ref{fig:activity-p3} représente la boucle principale~: pour chaque événement du sous-ensemble CSV assigné, le payload JSON est formaté puis publié sur le topic \texttt{iot/sensors/device-XXX/\{type\}}. Les événements étiquetés comme attaques adaptent leur comportement (accélération de fréquence pour DoS, duplication pour Replay, modification de payload pour MiTM). Le nœud de bouclage se poursuit jusqu'à épuisement des événements, après quoi le client se déconnecte proprement et les statistiques sont agrégées.

\begin{figure}[!htbp]
\centering
\includegraphics[width=0.72\linewidth]{diag-activity-part3.png}
\caption{Activité --- Phase~3~: boucle de publication MQTT et agrégation des résultats}
\label{fig:activity-p3}
\end{figure}
\FloatBarrier

\section{Planification temporelle}
\label{sec:co-gantt}

Le diagramme de Gantt (figure~\ref{fig:gantt}) consolide l'ensemble des sprints sur la durée totale du projet. L'axe horizontal représente le temps en semaines~; les barres horizontales correspondent aux périodes de réalisation de chaque sprint. On observe que les trois premiers sprints occupent la moitié de la durée totale et constituent le socle technique~: sans une PKI fonctionnelle (S2) et un broker sécurisé (S3), aucune donnée de trafic chiffré ne peut être générée ni analysée. Les flèches de dépendance illustrent les contraintes de précédence~: S4 ne démarre qu'après la validation du broker (fin S3), et S6 ne peut débuter sans les captures de trafic produites par S4--S5.

\begin{figure}[!htbp]
\centering
\includegraphics[width=\linewidth,height=0.66\textheight,keepaspectratio]{diag-gantt.png}
\caption{Diagramme de Gantt du projet}
\label{fig:gantt}
\end{figure}

\section{Conclusion}
\label{sec:co-conclusion}

Ce chapitre a formalisé la conception du projet en articulant successivement le diagnostic de l'existant, la spécification des besoins, la vue d'ensemble de la solution retenue, les artefacts de pilotage et la modélisation du système. Cet ensemble fournit une base cohérente pour la phase de réalisation, en assurant la traçabilité entre les lacunes identifiées, les objectifs visés et les choix de conception adoptés. Le chapitre suivant décrit précisément \textbf{comment} chaque élément a été mis en \oe{}uvre~: de l'environnement GNS3 à la PKI Vault, du broker mTLS au pipeline ML, en passant par les scénarios d'attaque.
